/**
 * Configuração de conexão com o backend.
 *
 * Depois de fazer o deploy do backend na sua VPS, troque a URL abaixo
 * pela URL real da sua API (ex: "https://api.seudominio.com").
 *
 * Este é o único arquivo que você precisa editar antes do deploy.
 */
window.PANEL_CONFIG = {
  API_BASE_URL: "https://SEU-BACKEND-AQUI.com",
};
